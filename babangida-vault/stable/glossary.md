# Глоссарий — ubiquitous language

Единый язык babangida: что значит термин в продукте и в коде. Описываем _термины_,
а не _типы_ — структуры (`Invite`, `Profile`, …) и инварианты живут в крейте
`domain` (ADR-0003). Здесь — смысл и границы понятия, одной строкой.

Формат записи: **термин** — краткое определение; при необходимости связь с ADR.
Наполняется по мере роста домена.

## Контекст identity-invites

- **invite** — приглашение, единственный путь регистрации. Правила выдачи — ADR-0005.
- **invite code** — код приглашения, по которому инвайт принимают. Генерится на границе, домен валидирует.
- **issuance (выдача)** — акт создания инвайта. Ограничен квотой и кулдауном (ADR-0005).
- **cooldown** — пауза между выдачами инвайтов (~12ч, ADR-0005). На админа не действует.
- **quota** — лимит одновременно активных (непринятых) инвайтов: 2; админ — без лимита (ADR-0005).
- **active invite** — выпущенный, ещё не принятый инвайт; только такие считаются в квоте.
- **handle** — уникальный @-идентификатор юзера (нормализуется в нижний регистр).
- **role** — роль юзера: **member** (рядовой) или **admin** (без лимита/кулдауна на инвайты).
- **verified** — статус верификации: открывает маркет, музыку, open API (ADR-0010).
- **casual** — (1) статус верификации: невериф. юзер (лента/профиль/переписка); (2) вариант субкультуры. Контекст различает.

## Контекст social

- **profile** — профиль юзера (display name, субкультура, био), один-к-одному с юзером.
- **display name** — отображаемое имя (не путать с `handle`).
- **subculture** — субкультура профиля: underground / casual / skin / hiphop / … (набор курируемый).
- **underground** — культурный контур комьюнити; задаёт тон продукта и модерации.
- **skin** — субкультура (вариант `subculture`); точный смысл закрепляется комьюнити.

## Контекст content

- **post** — запись юзера в ленте.
- **feed** — лента постов; читается через read-модель CQRS (ADR-0004), не через агрегаты.

## Контекст messaging

- **conversation (диалог)** — личная переписка двух участников (DM). Пара канонизируется: `(a,b)` и `(b,a)` — один диалог.
- **message** — сообщение в диалоге; писать может только участник (ADR-0012).
- **DM** — личное сообщение/диалог один-на-один. Доступно без верификации (лента/профиль/переписка — для casual, ADR-0010).

## Контекст community

- **community (сообщество)** — закрытая группа или паблик; контент течёт в ОБЩУЮ ленту, не в отдельное приложение (анти-ВК, ADR-0012).
- **closed (закрытая группа)** — читают и пишут только участники; вступление — по приглашению модератора.
- **public (паблик)** — читают все, публикуют только модераторы, вступление свободное (ADR-0012).
- **slug** — уникальный @-идентификатор сообщества в URL (правила как у `handle`).
- **membership role** — роль в сообществе: **owner** / **moderator** / **member**.
- **owner (владелец)** — владелец сообщества; всегда есть хотя бы один (инвариант ADR-0012).

## Контекст auth

- **credential (учётные данные)** — хэш пароля юзера, один-к-одному с юзером. Сырого пароля в домене нет (ADR-0013).
- **password** — сырой пароль с границы; проверяется по политике длины, никогда не сохраняется и не логируется. Хэшируется на границе (argon2id).
- **password hash** — непрозрачный результат хэшера; единственное, что хранит `credential`.
- **session (сессия)** — выпущенный логином доступ: строка в БД со сроком жизни (`SESSION_TTL` ~30 дней). Гасится логаутом (ADR-0013).
- **session token** — непрозрачный высокоэнтропийный секрет, который держит клиент (HttpOnly-кука / Bearer); по нему распознаётся текущий юзер.
- **current user (текущий юзер)** — юзер, распознанный по токену сессии; несёт `verified` для гейта привилегий (ADR-0010).

## Контекст marketplace

- **marketplace (барахолка)** — продажа вещей/железа внутри сети; витрина, сделка вне платформы (ADR-0014).
- **listing (товар)** — объявление о продаже: заголовок, цена, опц. описание, статус. Корень агрегата marketplace.
- **price (цена)** — целые рубли, строго положительная (MVP: без копеек/валют).
- **listing status** — статус товара: **active** (в продаже) / **sold** (продан) / **withdrawn** (снят). Меняет только продавец.
- **verification gate (гейт верификации)** — продавать может только верифицированный (ADR-0010); инвариант домена (`Listing::list`).
- **verify (выдать верификацию)** — админ-действие: открыть юзеру привилегированные зоны (маркет/музыка/API). Ручная модерация (ADR-0010).

## Контекст verification

- **verification request (заявка на верификацию)** — заявка юзера на статус verified. Корень агрегата verification; владеет процессом, не статусом (статус — на `User`, ADR-0016).
- **request status** — статус заявки: **pending** (ждёт админа) / **approved** (одобрена → юзер verified) / **rejected** (отклонена, можно подать новую). Машина состояний — инвариант домена.
- **note (записка)** — необязательное сопровождение заявки от юзера (кто он, ссылки на треки); 1..=500 симв.
- **decision reason (причина решения)** — необязательная причина одобрения/отказа от админа; 1..=500 симв.
- **review (рассмотрение)** — решение админа по заявке (`approve`/`reject`); одобрение атомарно ставит юзеру `verified` (ADR-0016).
- **one open request (одна открытая заявка)** — у юзера не более одной `pending` заявки одновременно (партиал-уникальный индекс + проверка в application, ADR-0016).
